version: 2

updates:
  # Keeps the SHA-pinned third-party actions current: Dependabot bumps both the
  # commit and the trailing "# vX.Y.Z" comment, so pins do not go stale.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(deps)"
